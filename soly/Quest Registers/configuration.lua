-- Enable addon
local enable = false
-- Default font color
local fontColor = 0xFFFFFFFF
-- Default font size
local fontSize = 1.0
-- Register changed color
local registerChangedColor = 0xFFFF0000
-- Register changed color duration
local registerChangedDuration = 2.0

return
{
    enable = enable,
    fontColor = fontColor,
    fontSize = fontSize,
    registerChangedColor = registerChangedColor,
    registerChangedDuration = registerChangedDuration,
}
