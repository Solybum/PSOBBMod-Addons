-- Enable addon window
local showPlayerReader = true
-- Default font color
local fontColor = 0xFFFFFFFF
-- Default font size
local fontSize = 1.0

return 
{
    showPlayerReader = showPlayerReader,
    fontColor = fontColor,
    fontSize = fontSize
}
