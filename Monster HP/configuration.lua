-- Enable addon window
local enable = true
-- Default font color
local fontColor = 0xFFFFFFFF
-- Default font size
local fontSize = 1.0

return
{
    enable = enable,
    fontColor = fontColor,
    fontSize = fontSize,
}
